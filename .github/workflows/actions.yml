name: CI

on:
  pull_request:
<<<<<<< HEAD
    paths-ignore:
      - "**.md"
      - "docs/**"
      - ".gitignore"
  push:
    branches: [develop]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v7
        with:
          python-version: "3.13"

      - name: Cache uv
        uses: actions/cache@v4
        with:
          path: ~/.cache/uv
          key: ${{ runner.os }}-uv-pre-commit
          restore-keys: |
            ${{ runner.os }}-uv-

      - name: Lint / format with pre-commit
        run: |
          uv pip install pre-commit --system
          pre-commit run -a

  test:
    runs-on: ubuntu-latest
    timeout-minutes: 15
=======
  push:
    branches: [develop]

jobs:
  test:
    runs-on: ubuntu-latest
>>>>>>> aa7ee908d69923833dae28f3c22652ec1337d2d5

    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: postgres
        ports: ["5432:5432"]
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
<<<<<<< HEAD
          --shm-size=256mb
=======
>>>>>>> aa7ee908d69923833dae28f3c22652ec1337d2d5

    steps:
      - name: Checkout
        uses: actions/checkout@v4

<<<<<<< HEAD
      - name: Install uv
        uses: astral-sh/setup-uv@v7
        with:
          python-version: "3.13"

      - name: Cache uv
        uses: actions/cache@v4
        with:
          path: ~/.cache/uv
          key: ${{ runner.os }}-uv-test
          restore-keys: |
            ${{ runner.os }}-uv-

      - name: Install dependencies
        run: uv pip install -r requirements-dev.txt --system
=======
      - name: Set up Python 3.13
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - name: Cache pip
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('requirements.txt', 'requirements-dev.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt -r requirements-dev.txt
>>>>>>> aa7ee908d69923833dae28f3c22652ec1337d2d5

      - name: Test with pytest
        env:
          DATABASE_URL: postgres://postgres:postgres@localhost:5432/postgres
          SECRET_KEY: for-testing-only
          DJANGO_SETTINGS_MODULE: config.test_settings
        run: |
<<<<<<< HEAD
          python -m pytest --cov --cov-report=html --cov-report=xml --junitxml=junit.xml -v

      - name: Upload HTML coverage report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: coverage-html
          path: htmlcov/
          retention-days: 30

      - name: Upload XML coverage report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: coverage-xml
          path: coverage.xml

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: pytest-results
          path: junit.xml
=======
          python -m pytest --tb=short -q

      # Add .pre-commit-config.yaml to the repo for this step to pass
      - name: Lint / format with pre-commit
        run: |
          pip install pre-commit
          pre-commit run -a
>>>>>>> aa7ee908d69923833dae28f3c22652ec1337d2d5
